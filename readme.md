Nodejs server to consume as client alphavantage endpoints
go to file alfavantage-client and replace 'demo' with your api key
```
const alfavantageApiKey = 'demo';
```

to deploy the app run
```
 npm run start
```